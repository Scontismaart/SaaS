Aggiungi una docstring in italiano alla funzione `create_booking` in `src/core/db/repository.py` (riga 13 circa).

Requisiti:
- Formato: docstring su una riga o piu' righe, in italiano, coerente con lo stile delle altre docstring gia' presenti nel file.
- Deve descrivere: cosa fa la funzione, i parametri principali e il valore di ritorno (la riga di ritorno, o eventualmente la riga / tuple di righe inserite).
- Non modificare la logica della funzione, i tipi, i nomi dei parametri o i commenti esistenti: SOLO aggiungere la docstring subito dopo la firma della funzione, prima del corpo.
- Non aggiungere commenti o modifiche fuori dalla funzione.
- Verifica di non introdurre errori di sintassi: esegui un controllo di importazione o lint sul file dopo la modifica.