Rendi timezone-aware il calcolo del cutoff in `get_review_analytics` in `src/core/db/repository.py` (riga 274 circa) e aggiungi test DAVVERO discriminanti. La run precedente ha prodotto un fix applicativo corretto ma test NON discriminanti (verificato dal redteam: tutti i test passavano anche ripristinando `datetime.utcnow()`).

Contesto: la funzione usa `datetime.utcnow()` (riga 276), deprecato (finding ruff DTZ003). Deve usare `datetime.now(timezone.utc)`. La colonna `created_at` di `reviews` e' TIMESTAMPTZ: il cutoff va calcolato in UTC aware per un confronto corretto. La funzione oggi non ha test.

Requisiti (TDD: prima i test, poi la modifica):

1. Refactor minimo applicativo: in `get_review_analytics`, sostituire `datetime.utcnow()` con `datetime.now(timezone.utc)` e aggiornare l'import locale a `from datetime import datetime, timedelta, timezone`. Nessun altro cambiamento a query, logica o formato del risultato. Non toccare `approve_review` (doppio `async with` intorno a riga 257-258 intenzionale, serve al lock transazionale).

2. Test discriminanti: il criterio di accettazione di QUESTA run e' che almeno un test FALLISCA con il codice vecchio (`datetime.utcnow()`) e passi con il nuovo. Servono DUE leve, entrambe obbligatorie:
   - Mock del tempo: `get_review_analytics` importa `datetime` LOCALMENTE dentro il corpo (`from datetime import datetime, timedelta, timezone`), quindi patchare `src.core.db.repository.datetime` NON intercetta la chiamata. Devi patchare i metodi della classe stdlib `datetime.datetime` (`now` e `utcnow`) tramite `unittest.mock.patch.object`, oppure iniettare un parametro opzionale di test. Fissa il valore restituito a un istante noto (es. `datetime(2026, 5, 1, 12, 0, 0, tzinfo=timezone.utc)`). L'approccio con `patch.object` e' preferito: nessuna modifica alla firma pubblica. Documenta nel report quale meccanismo hai usato e verifica empiricamente che il patch intercetti davvero la chiamata interna.
   - Sessione non-UTC a livello di POOL, non di singola connessione: `get_review_analytics` acquisisce una connessione propria dal pool, quindi un `SET TIME ZONE` eseguito su una connessione acquistata altrove nel test NON garantisce di agire su quella usata dalla funzione (il pool puo' riciclare qualsiasi connessione). Crea NEL TEST un pool dedicato con `asyncpg.create_pool(dsn, server_settings={"search_path": "public, extensions", "timezone": "Europe/Rome"})` (offset +2, `search_path` come nel conftest per trovare tabelle/estensioni), costruisci `CoreRepository(pool=pool_dedicato)` e usalo per INSERT e chiamata. Le tabelle esistono gia' nel container condiviso, quindi il pool dedicato le trova. Chiudi il pool a fine test (`await pool.close()`). Il motivo: con sessione UTC, un cutoff naive (`utcnow()`) e uno aware coincidono in istante e NON discriminano; con sessione +2, Postgres interpreta il naive come ora locale e sposta il confine della finestra di 2 ore.
   - Review a cavallo del confine: con il tempo mockato, calcola `cutoff_atteso = now_mock - timedelta(days=giorni)` (istante UTC aware). Inserisci review con `created_at` espresso esplicitamente in UTC aware:
     (a) `cutoff_atteso - 1 minuto` -> deve essere ESCLUSA;
     (b) `cutoff_atteso + 1 minuto` -> deve essere INCLUSA.
   - Assertions: la review (a) NON deve comparire in nessun bucket del risultato; la review (b) DEVE comparire. Con sessione +2 e cutoff naive, il confine effettivo in istante diventa `cutoff_atteso - 2h`, quindi la review (a) verrebbe erroneamente INCLUSA -> il test FALLISCE col codice vecchio e PASSA col nuovo. Questo e' il discriminator.
   - Verifica empirica obbligatoria: esegui i test col codice nuovo (deve passare), poi con `git stash` sul solo `src/core/db/repository.py` ripristina `datetime.utcnow()` e riesegui: ALMENO UN test del nuovo gruppo deve fallire. Poi `git stash pop`. Riporta l'esito di entrambe le esecuzioni nel report finale. Se il test discrimina (rosso sul vecchio, verde sul nuovo), il requisito e' soddisfatto.

3. Non introdurre dipendenze nuove: usa solo stdlib (`unittest.mock`, `datetime`), pytest e le fixture esistenti (`repo`, `sample_org`, `reset_db`). Se serve una INSERT con `created_at` esplicito, fai una INSERT diretta su `reviews` (come schema in `src/core/db/schema.sql:53-62`), NON chiamare `create_review` (non accetta `created_at`).

4. Verifica finale: esegui `python -m pytest tests/core/test_repository_reviews.py -v` (Docker attivo) finche' tutti i test passano; poi la suite completa `python -m pytest -v --tb=short`. Lint: `python -m ruff check src/core/db/repository.py tests/core/test_repository_reviews.py` non deve introdurre errori nuovi (i 3 preesistenti I001/SIM117/B017 restano fuori scope).